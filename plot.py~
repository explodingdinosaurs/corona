import pandas as pd
import plotly.graph_objects as go

df = pd.read_csv('total_cases.csv')
df = df.set_index('date')

fig = go.Figure()


for country in list(df):
    fig.add_trace(go.Scatter(x=df.index, y=df[country])

fig.show()
